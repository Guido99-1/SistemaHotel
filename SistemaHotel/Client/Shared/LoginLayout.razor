@* @inherits LayoutComponentBase


<MudThemeProvider />
<MudSnackbarProvider />

<MudContainer MaxWidth="MaxWidth.Small" Class="d-flex align-center" Style="height: 100vh;">
    <div class="d-flex flex-column mud-width-full">
        <MudPaper Elevation="25" Class="pa-8" Width="100%" MaxWidth="500px">
            @Body
        </MudPaper>
    </div>
</MudContainer>
 *@
@inherits LayoutComponentBase

<MudThemeProvider />
<MudSnackbarProvider />

<div class="login-bg">
    <MudContainer MaxWidth="MaxWidth.Small" Class="login-wrap">
        <MudPaper Elevation="18" Class="login-card pa-8">

            <!-- Logo -->
            <div class="login-logo">
                <img src="logoAgoyan.jpeg" alt="Agoyán" />
            </div>

            @Body
        </MudPaper>

        <!-- Footer opcional -->
        <div class="login-footer">
            <MudText Typo="Typo.caption" Class="mud-text-secondary">
                © @DateTime.Now.Year Hostería Agoyan
            </MudText>
        </div>
    </MudContainer>
</div>

<style>
    /* Fondo blanco-azul sutil */
    .login-bg {
        min-height: 100vh;
        display: flex;
        align-items: center;
        justify-content: center;
        padding: 24px;
        background: radial-gradient(900px 500px at 20% 10%, rgba(33,150,243,0.12), transparent 60%), radial-gradient(900px 500px at 80% 90%, rgba(25,118,210,0.10), transparent 55%), linear-gradient(180deg, #f7fbff 0%, #ffffff 60%, #f4f9ff 100%);
    }

    .login-wrap {
        display: flex;
        flex-direction: column;
        align-items: center;
        width: 100%;
    }

    /* Card */
    .login-card {
        width: 100%;
        max-width: 520px;
        border-radius: 18px;
        position: relative;
        overflow: hidden;
        background: rgba(255,255,255,0.92);
        backdrop-filter: blur(6px);
        border: 1px solid rgba(25,118,210,0.10);
    }

        /* línea superior sutil */
        /* .login-card::before {
            content: "";
            position: absolute;
            top: 0;
            left: 0;
            right: 0;
            height: 6px;
            background: linear-gradient(90deg, rgba(25,118,210,0.80), rgba(33,150,243,0.35));
        } */

    /* Logo */
    .login-logo {
        display: flex;
        justify-content: center;
        margin-bottom: 18px;
        margin-top: 6px;
    }

        .login-logo img {
            width: 140px;
            max-width: 60%;
            height: auto;
            object-fit: contain;
            filter: drop-shadow(0 10px 18px rgba(0,0,0,0.10));
        }

    /* Footer */
    .login-footer {
        margin-top: 14px;
        text-align: center;
        opacity: 0.9;
    }

    /* Mobile tweaks */
    @@media (max-width: 600px) {
        .login-card {
            padding: 22px !important;
            border-radius: 16px;
        }

        .login-logo img {
            width: 120px;
        }
    }
</style>
